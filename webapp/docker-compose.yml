services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: valorant
      POSTGRES_PASSWORD: valorant
      POSTGRES_DB: valorant_igl_tutor
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
